// @ts-check

/** @type {import('prettier').Options} */
const prettierConfig = {
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  quoteProps: 'as-needed',
  trailingComma: 'all',
  arrowParens: 'avoid',
};

module.exports = prettierConfig;
